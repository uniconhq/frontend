import { Badge } from "@/components/ui/badge";

export const DraftBadge = () => <Badge variant="orange">Draft</Badge>;
export const RestrictedBadge = () => <Badge variant="destructive">Restricted</Badge>;
